"""This subpackage holds all :class:`Instruments` measuring voltages, e fields.

:class:`field_probe` represents an electric field probe.
:class:`reconstructed` is the electric field at every time and position of the
coaxial, it is rebuilt from analytical laws fitted on the :class:`field_probe`.
Both these classes inherit from :class:`i_electric_field`.

"""
